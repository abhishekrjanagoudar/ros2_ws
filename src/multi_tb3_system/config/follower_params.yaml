# follower_node.py parameters — distances in m, velocities in m/s, angles in deg

follower_node:
  ros__parameters:

    target_distance:      0.7    # maintain this gap from robot ahead
    safe_distance:        0.4    # hard-stop if anything closer than this

    kp_linear:            0.8    # linear_vel  = kp_linear  * (dist - target_distance)
    kp_angular:           2.0    # angular_vel = kp_angular * angle_to_target

    max_linear_velocity:  0.22   # TB3 Burger hardware limit
    max_angular_velocity: 1.0

    front_angle_deg:      30.0   # ±deg cone ahead used for target detection
    cluster_distance:     0.20   # max gap between points in same cluster
    min_cluster_size:     2      # fewer → noise, rejected
    max_cluster_size:     40     # more  → wall, rejected (TB3 body ≈ 8-12 pts at 0.7m)
